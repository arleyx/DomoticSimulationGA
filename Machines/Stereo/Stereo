NAME=Stereo
STATES=onlineState,onState,offState,offlineState
STATE_INITIAL=offlineState
STATE_VALUE.onlineState=20w
STATE_VALUE.onState=85w
STATE_VALUE.offState=20w
STATE_VALUE.offlineState=0w
STATE_CONNECTION.onlineState=offlineState,onState
STATE_CONNECTION.onState=offState
STATE_CONNECTION.offState=onState,offlineState
STATE_CONNECTION.offlineState=onlineState
